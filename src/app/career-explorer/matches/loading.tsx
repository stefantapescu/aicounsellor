export default function Loading() {
  // Returning null means no specific loading UI will be shown instantly.
  // The browser will keep displaying the previous UI until the new page is ready.
  // Consider adding a skeleton UI here for better user experience if data fetching takes time.
  return null
}
